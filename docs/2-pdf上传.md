我的 /api/parse-pdf/route.ts 已存在。请检查其逻辑：1. 确保它使用 pdf-parse 正确解析上传的 File 对象。2. 修改前端的上传逻辑，当解析成功后，将返回的 text 自动填充到主输入框中。3. 如果解析出的内容过长，请在填充前弹窗提示用户。”


请帮我调整 page.tsx 的布局：
使用 Tailwind CSS 创建一个居中的最大宽度容器 (max-w-4xl)。
将现有文本框放在一个名为 InputGroup 的组件中，上方增加一个拖拽上传 PDF 的区域。
在输入框下方添加一个状态展示区，当 AI 正在生成时显示 Skeleton 加载效果。
生成成功后，隐藏输入框，全屏显示闪卡复习界面，并包含进度条（例如：5/20）和翻转动画。



“请在我的闪卡展示页面增加一个‘导出’按钮。
点击后弹出一个下拉菜单，选项包括：‘导出为 Anki (CSV)’ 和 ‘导出为 PDF’。
CSV 逻辑：将当前生成的 questions 和 answers 映射为两列，第一行为表头，点击后触发浏览器下载。
PDF 逻辑：调用 jsPDF 库，将卡片生成为适合打印的列表格式，每页 8 张卡片。”